{
  "name": "doc-templates-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "node scripts/prepare-docs.js && vitepress dev docs",
    "docs:build": "node scripts/prepare-docs.js && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:clean": "node scripts/clean-docs.js"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
